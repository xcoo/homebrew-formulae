# Xcoo / Homebrew Formulae

Xcoo's formulae for the [Homebrew][homebrew] package manager.

## Usage

`brew install xcoo/formulae/<formula>`

or `brew tap xcoo/formulae` and then `brew install <formula>`.

[homebrew]: http://brew.sh/
